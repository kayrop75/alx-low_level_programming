#include "main.h"
#include <stdio.h>

int main(void)
{
	    char *str = "Hello, world!";

	        printf("Original string: %s\n", str);
		    printf("Reversed string: ");
		        print_rev(str);

			    return 0;
}

